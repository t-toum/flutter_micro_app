include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    invalid_annotation_target: ignore
  exclude: 
    - "**/*.g.dart"
    - "**/*.freezed.dart"

# Coverage exclusions
coverage:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"